# scripts

Developer automation lives here.

Current notable entrypoints:

- `install-local.sh` installs the browser fallback preview utility
- `install-electron-local.sh` swaps the active local desktop launcher to the repo-owned Electron wrapper with rollback preserved
- `import-electron-payload.sh` imports the minimum local Electron payload slice into the ignored vendor area
